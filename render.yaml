services:
  - type: web
    name: investment-command
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    branch: main
    envVars:
      - key: NODE_ENV
        value: production
